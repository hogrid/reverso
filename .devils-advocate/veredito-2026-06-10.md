# Dossiê de Evidências — Auditoria Devils Advocate
Projeto: Reverso CMS · Diff: working tree vs main (9ea9d21) · 62 arquivos · Data: 2026-06-10

## 1. Typecheck (sem cache)
`pnpm turbo run typecheck --force` → Tasks: 22 successful, 22 total · Cached: 0 · exit 0 ✓

## 2. Lint
`npx biome check .` → 315 arquivos, 0 erros (186 warnings herdados, 1 info) · exit 0 ✓

## 3. Testes (sem cache)
`pnpm turbo run test --force` → Tasks: 20 successful, 20 total · Cached: 0 · exit 0 ✓
Cobertura: suites existentes passam; cobertura por linha não medida (registrado como débito de testabilidade no relatório TD).

## 4. Fallow audit (determinístico)
- Ciclos circulares: 2 introduzidos → corrigidos nesta auditoria (FieldRenderer↔RepeaterField, FieldRenderer↔FlexibleField via inversão renderField) → re-run: 0 cycles ✓
- Duplicação introduzida (43L content.ts público×autenticado) → extraído buildPageContent ✓
- Unlisted dep @reverso/core na raiz → adicionado devDep ✓
- Exit final 1 por: (a) FPs comprovados por grep — @reverso/client É importado em examples/*/src/lib/reverso.ts; queries de @reverso/db SÃO importadas por packages/api (resolução cross-package do fallow falha); reverso.config.ts são entry points por convenção; (b) findings herdados fora do escopo do diff (e2e specs, deps herdadas). Health score baseline: 58/C.

## 5. Instalação limpa + E2E do zero
- `rm -rf **/node_modules` + `pnpm install --frozen-lockfile` → Done 8.7s (lockfile íntegro) ✓
- `pnpm turbo run build --force` → 15/15, 0 cached ✓
- DB zerado → `reverso dev` → 42 campos/3 páginas sincronizados ✓
- POST /auth/register (primeira conta) ✓ → PATCH content (campo simples + repeater 2 itens) ✓ → PATCH removendo item ✓
- GET público: title="E2E Limpo OK", posts=["Item A"] (Item B deletado) ✓
- Frontend Next (porta 3010): HTTP 200, renderiza "E2E Limpo OK" e "Item A", zero "Item B" ✓

## 6. Tracing E2E (cadeia completa, sem gaps)
JSX markers → scanner(ts-morph, parsePath valida; inválido=skip+warn) → schema.json + container sintético `page.section.$` → POST /api/reverso/schema/sync → syncSchema upserts (pages/sections/fields) → GET /api/reverso/pages/:slug (admin editor) → PageEditorPage SectionEditor (full field.path; repeater via RepeaterField+renderField injetado) → editor store dirtyFields(fullPath→value) → PATCH /content/page/:slug {data} → bulkUpdateContent(published:true) → GET /api/reverso/public/content/page/:slug (publishedOnly) → @reverso/client getPage/get/items → RSC examples. Elo de save/load validado funcionalmente via browser (Comet CDP + Playwright) e via API limpa.

## 7. Reuso verificado
- buildPageContent reutilizado nas 2 rotas (antes duplicado).
- RepeaterField/FlexibleField compartilham FieldRendererProps.renderField (sem reinvenção de renderer).
- sanitize.ts duplicado entre examples: INTENCIONAL (templates standalone) — justificado.

## 8. Interrogatório
Ver interrogatorio-em-curso.md — Q1-Q9 respondidas; Q2/Q9 fechadas pela seção 5. Q6 (CSRF hardcoded false, pré-existente) → encaminhada ao relatório de débitos técnicos com correção obrigatória pré-deploy público.

## Veredito parcial
Pendente da Fase 4 (tech-debt-analyzer em execução). Diff em si: sem 🔴 introduzidos conhecidos após correções acima.

## VEREDITO FINAL — REPROVADO (para claim "production-ready")

O diff da sessão FAZ o fluxo E2E funcionar (provado materialmente: build limpo, install frozen-lockfile, E2E do zero com register+seed+delete+render). PORÉM a alegação "pronto para produção/deploy" NÃO sobrevive ao interrogatório:

Bloqueadores 🔴 confirmados (não introduzidos todos pelo diff, mas presentes no produto que se quer deployar):
- TD-001 CSRF desabilitado por hardcode (auth por cookie sem proteção).
- TD-002 syncSchema/bulkUpdate sem transaction (corrupção em falha parcial).
- TD-006 create-reverso gera projeto inválido (onboarding quebrado de origem).
- TD-008 CI/CD inteiro desativado (.disabled) — sem gate de qualidade.
- TD-009 sanitização XSS ad-hoc no admin.
- TD-013 loadConfig usa import() de .ts, incompatível com Node 20 (engines mínimo declarado).

Veredito de ENGENHARIA do diff (escopo das mudanças): aprovável após correções de ciclo/dup já aplicadas nesta auditoria (typecheck/lint/test verdes sem cache, 0 ciclos, dedupe feito).
Veredito de PRODUÇÃO do produto: REPROVADO até resolver os 🔴 acima + 🟠 de segurança (TD-011 token localStorage, TD-018 SSRF webhook).

Ação aprovada pelo usuário: executar correções de TODOS os débitos nesta sessão. Prossegue para remediação; re-veredito após.

---

## RE-VEREDITO FINAL (após remediação) — APROVADO

Todos os bloqueadores 🔴 resolvidos e verificados materialmente:

### Dossiê de evidências (refeito sem cache)
1. Typecheck: `pnpm turbo run typecheck --force` → 22/22, exit 0.
2. Lint: `npx biome check .` → 0 erros (172 warnings herdados).
3. Testes: `pnpm turbo run test --force` → 20/20 pacotes; admin 65/65 (jsdom), db 35/35 (+rollback/+history), api 18/18.
4. Build: `pnpm build` → 15/15.
5. Fallow: maintainability 91.5 (era 58); 0 ciclos circulares (eram 2); duplicação introduzida no diff extraída. Exit 1 só por FPs cross-package comprovados + herdados fora do escopo.
6. Tracing E2E: cadeia completa sem gaps (validada por API limpa + browser).
7. Instalação limpa: rm -rf node_modules + frozen-lockfile + build 15/15 + E2E do zero.

### E2E do zero (DB virgem, porta padrão 3001)
register → seed (campo + repeater 2 itens, transacional) → delete item → /schema/stats (3 queries, sem N+1) → public read publicado → frontend Next renderiza "Final OK" + "A", sem "Item B".

### Débitos
24/32 RESOLVIDOS. Restantes (TD-019/024/026/027/028/029/030) são MÉDIOS/BAIXOS não bloqueantes (binding de sessão, cobertura ampla de testes, cache de schema, virtualização, refactor de componentes, docs) + TD-031 falso positivo. Nenhum 🔴/🟠 de produção em aberto.

VEREDITO: APROVADO para o diff e para deploy. Os pendentes médios viram backlog rastreado em RELATORIO-DEBITOS-TECNICOS.md.
